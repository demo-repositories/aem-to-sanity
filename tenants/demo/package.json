{
  "name": "tenant-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Offline demo tenant — replays scrubbed AEM fixtures through the full migration pipeline.",
  "type": "module",
  "scripts": {
    "migrate:schema": "aem-to-sanity-schema",
    "typegen": "aem-to-sanity-typegen",
    "pagebuilder:refresh": "aem-to-sanity-pagebuilder",
    "extract": "aem-extract",
    "tags": "aem-tags",
    "transform": "aem-transform --registry ./output/cache/content-type-registry.json",
    "assets": "aem-assets",
    "import": "aem-import",
    "migrate:demo": "aem-to-sanity run \"pnpm run extract && pnpm run tags && pnpm run migrate:schema && pnpm run transform && pnpm run assets && pnpm run import -- --discard-drafts\"",
    "migrate:content": "aem-to-sanity run \"pnpm run extract && pnpm run tags && pnpm run transform && pnpm run assets && pnpm run import\"",
    "migrate:all": "pnpm run migrate:schema && pnpm run typegen",
    "migrate": "aem-to-sanity run \"pnpm run extract && pnpm run tags && pnpm run migrate:schema && pnpm run transform && pnpm run assets && pnpm run import -- --discard-drafts\""
  },
  "devDependencies": {
    "aem-to-sanity-cli": "workspace:*",
    "aem-to-sanity-content": "workspace:*",
    "aem-to-sanity-schema": "workspace:*",
    "sanity": "^6.6.0",
    "tsx": "^4.23.1"
  }
}
