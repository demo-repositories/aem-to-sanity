# Sanity Studio auto-loads this file when you run `sanity dev` or `sanity build`.
# The SANITY_STUDIO_ prefix is the officially supported way to expose values to
# the browser bundle. The unprefixed names are also accepted as fallbacks so
# you can share a single .env with the content-migration CLI.

SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production

# Optional — required only for `sanity deploy` (studio hosting).
# Find in the Studio settings page, or let `sanity deploy` create one on first run.
SANITY_STUDIO_APP_ID=

# Bynder-backed migrations (MIGRATION_ASSET_BACKEND=bynder) only: setting the
# portal URL activates sanity-plugin-bynder-input via the `aemBynderPlugin()`
# spread in sanity.config.ts — it registers the `bynder.asset` type the
# generated schemas reference and gives authors the Bynder Compact View to
# browse/pick assets. Leave unset for Media Library migrations (inert).
# SANITY_STUDIO_BYNDER_PORTAL_URL=https://<your-portal>.bynder.com
# Compact View UI language (default en_US):
# SANITY_STUDIO_BYNDER_LANGUAGE=en_US
